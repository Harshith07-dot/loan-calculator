import React from 'react';
import { Typography } from '@mui/material';

const About = () => (
  <Typography>
    This is a Loan Calculator SPA built using React and Material UI. It calculates EMI, shows amortization, and live currency exchange rates.
  </Typography>
);

export default About;
